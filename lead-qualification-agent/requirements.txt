# Core dependencies
pydantic>=2.0.0
streamlit>=1.28.0

# Web search for enrichment fallback
ddgs>=9.0.0

# LLM providers (install at least one)
# anthropic>=0.30.0
# openai>=1.0.0

# LangGraph (optional - fallback to simple pipeline if not installed)
# langgraph>=0.0.20
# langgraph-checkpoint>=0.0.10

# Testing
pytest>=7.0.0
pytest-mock>=3.10.0